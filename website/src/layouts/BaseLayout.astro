---
interface Props {
  title?: string;
  description?: string;
}

const { title = 'OpenSpace — Terminal Workspace', description = 'A premium, open-source terminal workspace for developers. Multiple terminals, built-in browser, keyboard-first design.' } = Astro.props;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Sora:wght@600;700;800&family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet" />
    <link rel="icon" type="image/png" href="/images/icon.png" />

    <style>
      @import "tailwindcss";

      @theme {
        --font-display: 'Sora', sans-serif;
        --font-sans: 'DM Sans', sans-serif;
        --font-mono: 'JetBrains Mono', monospace;

        --color-deep: #000000;
        --color-dark: #050505;
        --color-surface: #0a0a0a;
        --color-card: #0f0f0f;
        --color-ember: #FB6D3A;
        --color-ember-dark: #E04F1A;
        --color-ember-light: #FF8A5C;
        --color-foreground: #f0f0f0;
        --color-muted: #888888;
        --color-subtle: #555555;
        --color-border: #181818;
        --color-border-hover: #2a2a2a;
      }

      body {
        background: var(--color-deep);
        color: var(--color-foreground);
        font-family: var(--font-sans);
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
      }

      ::selection {
        background: rgba(251, 109, 58, 0.3);
        color: #fff;
      }

      .glow-ember {
        box-shadow: 0 0 30px rgba(251, 109, 58, 0.08), 0 0 60px rgba(251, 109, 58, 0.04);
      }

      .text-gradient-ember {
        background: linear-gradient(135deg, #FB6D3A, #FF8A5C, #E04F1A);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .bg-grid {
        background-image:
          linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
          linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
        background-size: 64px 64px;
      }

      .card-hover {
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
      }
      .card-hover:hover {
        transform: translateY(-3px);
        border-color: var(--color-border-hover);
        background: rgba(255, 255, 255, 0.02);
      }

      .reveal {
        opacity: 0;
        transform: translateY(30px);
        transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1), transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
      }
      .reveal.visible {
        opacity: 1;
        transform: translateY(0);
      }
      .reveal-scale {
        opacity: 0;
        transform: scale(0.95);
        transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1), transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
      }
      .reveal-scale.visible {
        opacity: 1;
        transform: scale(1);
      }

      @keyframes fadeInUp {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
      }
      @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
      }
      @keyframes blink {
        0%, 100% { opacity: 1; }
        50% { opacity: 0; }
      }
      @keyframes pulse-ember {
        0%, 100% { box-shadow: 0 0 20px rgba(251, 109, 58, 0); }
        50% { box-shadow: 0 0 30px rgba(251, 109, 58, 0.15); }
      }

      .animate-fade-in-up { animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards; }
      .animate-fade-in { animation: fadeIn 1s ease-out forwards; }
      .animate-blink { animation: blink 1s step-end infinite; }
      .animate-pulse-ember { animation: pulse-ember 3s ease-in-out infinite; }
      .stagger-0 { animation-delay: 0s; }
      .stagger-1 { animation-delay: 0.15s; }
      .stagger-2 { animation-delay: 0.3s; }
      .stagger-3 { animation-delay: 0.45s; }
      .stagger-4 { animation-delay: 0.6s; }

      .watermark {
        position: absolute;
        font-family: var(--font-display);
        font-weight: 800;
        font-size: clamp(10rem, 25vw, 22rem);
        color: rgba(255, 255, 255, 0.02);
        line-height: 1;
        pointer-events: none;
        user-select: none;
        letter-spacing: -0.04em;
      }
    </style>
  </head>
  <body class="bg-deep text-foreground antialiased">
    <slot />
  </body>
</html>
