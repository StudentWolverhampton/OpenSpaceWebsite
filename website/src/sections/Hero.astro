<section class="bg-grid relative flex min-h-screen items-center justify-center overflow-hidden pt-16">
  <canvas id="starfield" class="absolute inset-0 h-full w-full"></canvas>

  <div class="absolute inset-0 bg-gradient-to-b from-cyber/3 via-transparent to-space"></div>
  <div class="absolute top-1/4 left-1/2 -translate-x-1/2 -translate-y-1/2 h-[600px] w-[600px] rounded-full bg-cyber/3 blur-3xl"></div>
  <div class="absolute bottom-1/3 right-1/4 h-[400px] w-[400px] rounded-full bg-amber/3 blur-3xl"></div>

  <div class="relative z-10 mx-auto max-w-5xl px-6 text-center">
    <div class="animate-fade-in-up stagger-0 mb-6">
      <span class="inline-flex items-center gap-2 rounded-full border border-cyber/20 bg-cyber/5 px-5 py-1.5 text-sm font-medium text-cyber backdrop-blur-sm">
        <span class="relative flex h-2 w-2">
          <span class="absolute inline-flex h-full w-full animate-ping rounded-full bg-cyber opacity-75"></span>
          <span class="relative inline-flex h-2 w-2 rounded-full bg-cyber"></span>
        </span>
        Now available for Windows
      </span>
    </div>

    <h1 class="animate-fade-in-up stagger-1 mb-6 text-5xl font-extrabold leading-tight tracking-tight font-display sm:text-6xl md:text-7xl lg:text-8xl">
      Terminal Workspace.<br />
      <span class="text-gradient" id="typewriter-line">Reimagined</span><span class="animate-fade-in stagger-3 inline-block h-[1ex] w-[3px] animate-pulse bg-cyber align-middle"></span>
    </h1>

    <p class="animate-fade-in-up stagger-2 mx-auto mb-12 max-w-2xl text-base leading-relaxed text-muted sm:text-lg">
      A premium, open-source terminal workspace for developers who demand more.
      Multiple terminals, built-in browser, keyboard-first — all in one place.
    </p>

    <div class="animate-fade-in-up stagger-3 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">
      <a
        href="#download"
        class="group relative inline-flex items-center gap-3 rounded-xl bg-gradient-to-r from-cyber to-cyber-dark px-8 py-4 text-base font-semibold text-space transition-all hover:shadow-[0_0_30px_rgba(34,211,238,0.4)] hover:scale-105 active:scale-95"
      >
        <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
        </svg>
        Download Free
        <span class="rounded-md bg-white/15 px-2 py-0.5 text-xs font-mono">v1.0.0</span>
        <div class="absolute inset-0 -translate-x-full bg-gradient-to-r from-transparent via-white/15 to-transparent transition-transform duration-700 group-hover:translate-x-full"></div>
      </a>

      <a
        href="#features"
        class="inline-flex items-center gap-2 rounded-xl border border-border bg-white/5 px-8 py-4 text-base font-semibold text-foreground backdrop-blur-sm transition-all hover:bg-white/10 hover:border-cyber/30 active:scale-95"
      >
        Learn More
        <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
        </svg>
      </a>
    </div>

    <div class="animate-fade-in stagger-4 mt-16 flex items-center justify-center gap-8 text-xs text-muted">
      <span class="flex items-center gap-2">
        <svg class="h-4 w-4 text-cyber/60" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z" />
        </svg>
        Open-source
      </span>
      <span class="flex items-center gap-2">
        <svg class="h-4 w-4 text-cyber/60" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        Cross-platform
      </span>
      <span class="flex items-center gap-2">
        <svg class="h-4 w-4 text-cyber/60" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 3c2.755 0 5.455.232 8.083.678.533.09.917.556.917 1.096v1.044a2.25 2.25 0 01-.659 1.591l-5.432 5.432a2.25 2.25 0 00-.659 1.591v2.927a2.25 2.25 0 01-1.244 2.013L9.75 21v-6.568a2.25 2.25 0 00-.659-1.591L3.659 7.409A2.25 2.25 0 013 5.818V4.774c0-.54.384-1.006.917-1.096A48.32 48.32 0 0112 3z" />
        </svg>
        Real PTY
      </span>
    </div>
  </div>

  <div class="absolute bottom-0 left-0 right-0 h-40 bg-gradient-to-t from-space to-transparent"></div>
</section>

<script>
  const canvas = document.getElementById('starfield');
  if (canvas) {
    const ctx = canvas.getContext('2d');
    let stars = [];
    let w, h;

    function resize() {
      w = canvas.width = canvas.offsetWidth;
      h = canvas.height = canvas.offsetHeight;
    }
    resize();
    window.addEventListener('resize', resize);

    for (let i = 0; i < 180; i++) {
      stars.push({
        x: Math.random() * w,
        y: Math.random() * h,
        size: Math.random() * 2 + 0.5,
        speed: Math.random() * 0.15 + 0.02,
        opacity: Math.random() * 0.8 + 0.2,
        twinkleSpeed: Math.random() * 0.02 + 0.005,
        twinklePhase: Math.random() * Math.PI * 2,
      });
    }

    function draw() {
      ctx.clearRect(0, 0, w, h);
      const time = Date.now() * 0.001;
      for (const s of stars) {
        s.y -= s.speed;
        if (s.y < -5) { s.y = h + 5; s.x = Math.random() * w; }
        const alpha = s.opacity * (0.6 + 0.4 * Math.sin(time * s.twinkleSpeed + s.twinklePhase));
        ctx.beginPath();
        ctx.arc(s.x, s.y, s.size, 0, Math.PI * 2);
        ctx.fillStyle = `rgba(34, 211, 238, ${alpha * 0.8})`;
        ctx.fill();
        if (s.size > 1.2) {
          ctx.beginPath();
          ctx.arc(s.x, s.y, s.size * 2.5, 0, Math.PI * 2);
          ctx.fillStyle = `rgba(34, 211, 238, ${alpha * 0.06})`;
          ctx.fill();
        }
      }
      requestAnimationFrame(draw);
    }
    draw();
  }
</script>
